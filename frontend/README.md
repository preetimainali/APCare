# Frontend — Hospital Command Center

Next.js + Tailwind + Recharts. Responsive UI for census, occupancy, trends, forecasts, alerts, and sharing.

- `app/` — App Router pages and layouts
- `components/` — Reusable UI (charts, cards, filters)
- `lib/` — API client, utils, types
